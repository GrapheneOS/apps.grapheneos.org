#!/bin/bash

set -o errexit -o nounset -o pipefail

rm -rf apps-tmp
cp -a apps apps-tmp

brotli_k() {
    brotli -k "$@"
}
export -f brotli_k

find apps-tmp -regex '.+\.\(json\|apk\)' |
    parallel -q ::: brotli_k zopfli :::: -
